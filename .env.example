# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real tokens.
#
# Used by VS Code REST Client in *.dev.http files via {{$dotenv VARIABLE_NAME}}.
#
# To get a token for the dev Azure environment, use Postman OAuth2 flow
# (see docs/operations/local-development.md §6) or dotnet user-jwts for local DevJwt mode:
#   dotnet user-jwts create --project src/backend/EventHub.Api --role Organizer --claim "oid=dev-user-1" --output token

# Target environment URL — switch between dev/test/local without editing .http files
# dev:   https://eventhub-dev-api.azurewebsites.net
# test:  https://eventhub-test-api.azurewebsites.net
# local: http://localhost:5165
EVENTHUB_HOST=https://eventhub-dev-api.azurewebsites.net

# Bearer token — get via Postman OAuth2 flow or dotnet user-jwts (local DevJwt mode)
# See docs/operations/local-development.md §6 for instructions
EVENTHUB_BEARER_TOKEN=<paste-token-here>
